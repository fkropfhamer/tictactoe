export enum BoardState {
    Empty = 0,
    X = 1,
    O = 2,
};

export enum Color {
    Black = "black",
    White = "white",
};

export enum EndingState {
    Tie = 0,
    OWin = 1,
    XWin = -1,
}
